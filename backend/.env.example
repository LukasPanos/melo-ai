# Optional. The current dataset-only flow does not call the Spotify API,
# so these are not required to run the backend. Keep here in case you
# extend the app to look up tracks outside the local CSV.
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
